{
    "hooks": {
        "after:bump": "./.github/workflows/helpers/update-saucectl-template.sh"
    },
    "git": {
        "commitMessage": "Release v${version}",
        "requireBranch": "master",
        "tag": true,
        "tagName": "v${version}"
    },
    "npm": {
        "publish": false
    },
    "github": {
        "draft": true,
        "releaseName": "v${version}",
        "releaseNotes": "./.github/workflows/helpers/generate-release-notes.sh ${latestVersion} ${version}"
    }
}